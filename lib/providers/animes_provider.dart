import 'package:flutter/material.dart';

class AnimesProvider extends StatelessWidget {
  const AnimesProvider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
